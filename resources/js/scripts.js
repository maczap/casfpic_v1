function copiarCdBarra() {
    /* Get the text field */
    var copyText = document.getElementById("cd_barra");
  
    /* Select the text field */
    copyText.select();
    copyText.setSelectionRange(0, 99999); /* For mobile devices */
  
     /* Copy the text inside the text field */
    navigator.clipboard.writeText(copyText.value);
  

  }

  function copiarLinkPromotor() {
    /* Get the text field */
    var copyText = document.getElementById("link_promotor");
  
    /* Select the text field */
    copyText.select();
    copyText.setSelectionRange(0, 99999); /* For mobile devices */
  
     /* Copy the text inside the text field */
    navigator.clipboard.writeText(copyText.value);
  

  }  

  function copiarLinkAdmBoleto() {
    /* Get the text field */
    var copyText = document.getElementById("adm_boleto");
  
    /* Select the text field */
    copyText.select();
    copyText.setSelectionRange(0, 99999); /* For mobile devices */
  
     /* Copy the text inside the text field */
    navigator.clipboard.writeText(copyText.value);
  

  }    

  function copiarLinkAdmBarra() {
    /* Get the text field */
    var copyText = document.getElementById("adm_barra");
  
    /* Select the text field */
    copyText.select();
    copyText.setSelectionRange(0, 99999); /* For mobile devices */
  
     /* Copy the text inside the text field */
    navigator.clipboard.writeText(copyText.value);
  

  }     


  function copiarLinkAdmPix() {
    /* Get the text field */
    var copyText = document.getElementById("adm_pix");
  
    /* Select the text field */
    copyText.select();
    copyText.setSelectionRange(0, 99999); /* For mobile devices */
  
     /* Copy the text inside the text field */
    navigator.clipboard.writeText(copyText.value);
  

  }    